# TODO - AINav 复现与 GO2 落地计划

## 0. 当前目标
- 先完成仿真端的“可稳定复现”版本（简化重规划即可）
- 再迁移到 GO2 实体机器人执行

---

## 1. 仿真阶段（先做）

### 1.1 流程跑通（端到端）
- [ ] 启动仿真器并确认 ROS 话题通信正常
- [ ] 通过 `Interactive_Module/interactive.py` 输入任务，完成一次完整执行
- [ ] 确认 `LLM_Module` 主链路正常：`high_level -> low_level -> adaptive_controller`

完成标准：
- 至少 3 条不同指令可执行结束，不崩溃

### 1.2 输入结构对齐（input1/input2/input3）
- [ ] `input1`：用户文本指令稳定传入规划器
- [ ] `input2`：环境快照中包含 `position/environment_version/obstacles` 等关键字段
- [ ] `input3`：从技能列表动态提取可用技能与参数 schema

完成标准：
- 日志中可看到三类输入都被使用，且字段完整

### 1.3 简化重规划闭环验证
- [ ] 正常场景：任务无重规划直接完成
- [ ] 环境变化场景：触发 branch replan
- [ ] 连续失败场景：触发 replan 或重试上限退出

完成标准：
- 至少 2 个案例触发重规划且能够继续执行

### 1.4 仿真任务集（对应论文思路）
- [ ] Box obstruction（推开障碍）
- [ ] Box usage（利用箱子辅助通过）
- [ ] Integrated（混合任务）

完成标准：
- 每类任务至少跑 5 次，记录成功率和总耗时

### 1.5 结果记录与对比
- [ ] 记录 SR（成功率）、OT（总耗时）
- [ ] 统计触发重规划次数和主要触发原因
- [ ] 输出简要复现报告（可先 markdown）

完成标准：
- 形成一份可复查的仿真结果表

---

## 2. 实机迁移准备（仿真通过后）

### 2.1 技能接口替换（仿真 -> GO2）
- [ ] `move_forward/turn/stop` 改为 GO2 实机控制接口
- [ ] 增加统一安全限幅（最大线速度/角速度）
- [ ] 增加通信超时自动 `stop`

完成标准：
- 不依赖仿真队列，能在实机接口层单独调通

### 2.2 实机环境感知接入
- [ ] `build_env_state_snapshot()` 改为读取 GO2 实际状态（里程计/IMU/感知）
- [ ] 统一输出为当前控制器可用字段
- [ ] 增加 `environment_version` 更新策略

完成标准：
- 静态站立与移动时，状态字段持续更新

### 2.3 实机安全测试
- [ ] 急停逻辑测试（人工触发）
- [ ] 速度边界测试（不超过设定阈值）
- [ ] 异常恢复测试（传感器掉线/控制失败）

完成标准：
- 关键异常均可安全停机

---

## 3. 最终验收目标
- [ ] 仿真复现版本稳定运行
- [ ] GO2 上可完成至少 1 个交互导航任务
- [ ] 简化重规划在实机上可触发并生效

---

## 4. 你先看的建议顺序
1. 先看第 1.1 ~ 1.3（主链路和重规划是否真的有效）
2. 再看第 1.4 ~ 1.5（任务覆盖和结果记录）
3. 最后再进入第 2 部分（实机迁移）
