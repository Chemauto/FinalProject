# 🤖 FinalProject - 基于双层LLM的机器人控制系统

基于双层 LLM 架构的机器人控制系统，采用 MCP (Model Context Protocol) 模块化设计，使用 **ROS2 话题**进行通信。

## 🎯 核心特性

- **双层 LLM 架构**: 任务规划 + 执行控制分离
- **MCP 模块化设计**: 参考 RoboOS，工具注册标准化
- **ROS2 通讯**: 使用 ROS2 话题，标准化通信
- **自然语言控制**: 支持中文指令控制机器人
- **2D 仿真**: 基于 Pygame 的轻量级仿真环境
- **追击功能**: 自动追击敌人，PID控制精确定位
- **视觉检测**: VLM 颜色检测与动作映射
- **懒加载设计**: 自动初始化，零配置

## 🔄 系统架构与数据流

```
用户输入: "追击敌人" 或 "前进1米然后左转90度"
    ↓
┌─────────────────────────────────────────────┐
│ ① Interactive_Module (交互界面)             │
│    - 接收用户自然语言指令                     │
│    - 协调 LLM 和 Robot_Module                │
│    - 显示执行结果                             │
└──────────────┬──────────────────────────────┘
               ↓
┌─────────────────────────────────────────────┐
│ ② LLM_Module - 上层LLM (任务规划)           │
│    输入: 用户指令 + 可用技能列表              │
│    输出: 子任务序列 ( [{"task": "追击敌人"}] ) │
└──────────────┬──────────────────────────────┘
               ↓
┌─────────────────────────────────────────────┐
│ ② LLM_Module - 下层LLM (执行控制)           │
│    输入: 单个子任务描述                       │
│    输出: 工具调用 (chase_enemy() / move_forward()) │
└──────────────┬──────────────────────────────┘
               ↓
┌─────────────────────────────────────────────┐
│ ③ Robot_Module (MCP 工具注册中心)           │
│    ├─ skill.py: FastMCP 服务器              │
│    ├─ module/base.py: 移动/旋转/停止技能    │
│    ├─ module/chase.py: 追击技能             │
│    └─ module/vision.py: 视觉检测技能        │
│    输入: 工具调用请求                         │
│    输出: 动作指令 JSON ({"action": "move_forward", ...}) │
└──────────────┬──────────────────────────────┘
               ↓
      ROS2 Topic (/robot/command, /robot/enemies, /robot/state)
         ↓ 发布 JSON 消息
┌─────────────────────────────────────────────┐
│ ④ Sim_Module (2D 仿真器)                    │
│    ├─ 订阅 ROS2 话题                         │
│    ├─ 机器人运动可视化                       │
│    ├─ 敌人管理 (spawn/move/remove)           │
│    └─ 状态发布 (robot/enemy positions)       │
└─────────────────────────────────────────────┘
```

## 📁 项目结构

```
FinalProject/
├── README.md              # 主文档
├── requirements.txt       # Python 依赖
├── .env                   # API 密钥配置
│
├── LLM_Module/            # 双层LLM核心
│   ├── llm_core.py        # LLMAgent: 规划 + 执行
│   └── prompts/           # YAML 提示词模板
│       └── planning_prompt_2d.yaml
│
├── VLM_Module/            # 视觉语言模型
│   ├── vlm_core.py        # 本地 VLM (Ollama)
│   ├── vlm_core_remote.py # 远程 VLM (API)
│   └── prompts/           # VLM 提示词
│
├── Robot_Module/          # MCP 工具注册中心
│   ├── skill.py           # FastMCP 服务器入口
│   └── module/            # 功能模块
│       ├── base.py        # 底盘控制 (move/turn/stop)
│       ├── chase.py       # 追击功能 (chase_enemy)
│       └── vision.py      # 视觉检测 (detect_color)
│
├── Interactive_Module/    # 交互界面
│   └── interactive.py     # CLI 交互主程序
│
├── Sim_Module/            # 仿真模块
│   └── sim2d/
│       ├── simulator.py   # 主仿真器
│       ├── robot.py       # 机器人类
│       └── enemy.py       # 敌人类
│
├── Test_Module/           # 测试模块
│   ├── chase_core.py      # 追击算法核心
│   ├── enemy_manager.py   # 敌人管理器
│   └── chase_simulator.py # 追击仿真器 (简化版)
│
└── ros_topic_comm.py      # ROS2 通讯模块
    ├── /robot/command     # 动作命令
    ├── /robot/state       # 机器人状态
    ├── /robot/enemies     # 敌人位置
    └── /robot/enemy_remove # 敌人清除
```

## 🚀 快速开始

### 环境要求

- Python 3.10+
- Linux (推荐 Ubuntu 22.04)
- ROS2 Humble (可选，用于调试)

### 安装

```bash
# 1. 克隆项目
cd /home/robot/work/FinalProject

# 2. 创建 conda 环境 (可选)
conda create -n ros2_env python=3.10
conda activate ros2_env

# 3. 安装 ROS2 Humble (如果未安装)
# 参考: https://docs.ros.org/en/humble/Installation.html

# 4. 安装 Python 依赖
pip install -r requirements.txt

# 5. 配置 API Key
export Test_API_KEY=your_api_key_here
```

### 运行

```bash
# 一键启动 (推荐)
./start_robot_system.sh

# 或手动启动
# 终端1: python3 Sim_Module/sim2d/simulator.py
# 终端2: python3 Interactive_Module/interactive.py
```

### 交互示例

#### 基础运动控制

```
💬 请输入指令: 前进1米然后左转90度

[上层LLM] 任务规划:
  步骤1: 前进1米
  步骤2: 左转90度

[下层LLM] 执行控制:
  调用工具: move_forward(distance=1.0) ✅
  调用工具: turn(angle=90.0) ✅

📊 [完成] 2/2 个任务成功
```

#### 追击敌人

```
💬 请输入指令: 追击敌人

[上层LLM] 任务规划:
  步骤1: 追击最近的敌人

[下层LLM] 执行控制:
  调用工具: chase_enemy()
  [追击] 找到敌人 1 at (612, 114)
  [追击] 距离: 282.0 像素 (2.82 米)
  [追击] 旋转 41.3°
  [追击] 前进 0.50 米 (PID控制)
  [追击] 距离: 233.3 → 184.6 → 136.0 → 87.3 → 4.2 像素
  [追击] ✓ 已到达目标！误差 4.2 像素
  [追击] 清除敌人: 1 ✅

📊 [完成] 1/1 个任务成功
```

## 🔧 添加新工具

1. **创建模块文件**
```bash
cd Robot_Module/module
cp example.py your_module.py
```

2. **编写工具函数**
```python
async def your_tool(param: float) -> str:
    """工具描述

    Args:
        param: 参数描述
    """
    # ROS 队列会自动初始化
    from .base import _get_action_queue
    action = {'action': 'your_action', 'parameters': {'param': param}}
    _get_action_queue().put(action)
    return json.dumps(action)
```

3. **注册到 skill.py**
```python
# skill.py
from module.your_module import register_tools as register_your_tools

def register_all_modules():
    register_your_tools(mcp, _tool_registry, _tool_metadata)
```

## 📦 模块说明

| 模块 | 职责 | 核心文件 |
|------|------|----------|
| **Interactive_Module** | 交互界面，CLI主程序 | `interactive.py` |
| **LLM_Module** | 双层LLM: 规划+执行 | `llm_core.py` |
| **Robot_Module** | MCP工具注册中心 | `skill.py` |
| ├─ base.py | 底盘控制 | move/turn/stop |
| ├─ chase.py | 追击功能 | chase_enemy/chase_target |
| └─ vision.py | 视觉检测 | detect_color_and_act |
| **Sim_Module** | 2D仿真环境 | `sim2d/simulator.py` |
| **Test_Module** | 追击算法与测试 | `chase_core.py` |
| **VLM_Module** | 视觉语言模型 | `vlm_core.py` |

详细说明请查看各模块的 README.md。

## 📡 ROS2 通讯

### 话题列表

| 话题名称 | 消息类型 | 用途 |
|---------|---------|------|
| `/robot/command` | String | 动作命令 (move_forward/turn/stop) |
| `/robot/state` | String | 机器人状态 (x, y, angle) |
| `/robot/enemies` | String | 敌人位置列表 |
| `/robot/enemy_remove` | String | 敌人清除命令 |

### 消息格式

**动作命令** (`/robot/command`):
```json
{
  "action": "move_forward",
  "parameters": {
    "distance": 1.0,
    "speed": 0.3
  }
}
```

**机器人状态** (`/robot/state`):
```json
{
  "x": 400.0,
  "y": 300.0,
  "angle": 45.0
}
```

**敌人位置** (`/robot/enemies`):
```json
[
  {"id": "1", "x": 612, "y": 114},
  {"id": "2", "x": 200, "y": 400}
]
```

**敌人清除** (`/robot/enemy_remove`):
```json
{
  "enemy_id": "1"
}
```

### 调试命令

```bash
# 查看话题列表
ros2 topic list

# 查看话题消息
ros2 topic echo /robot/command

# 查看话题信息
ros2 topic info /robot/command

# 查看节点图
ros2 run rqt_graph rqt_graph
```

## 🎯 追击功能使用说明

### 启动追击仿真器

```bash
# 方式1: 使用简化版追击仿真器 (推荐用于测试)
python3 Test_Module/chase_simulator.py

# 方式2: 使用主仿真器
python3 Sim_Module/sim2d/simulator.py
```

### 追击仿真器操作

- **鼠标左键**: 在点击位置生成敌人
- **按 C**: 清除所有敌人
- **按 L**: 切换追击线显示
- **按 ESC**: 退出

### 追击命令

在交互程序中输入以下命令：

```bash
# 追击最近的敌人
追击敌人

# 追击指定坐标
追击坐标 (700, 300)

# 获取敌人位置
获取敌人位置
```

### 追击参数配置

在 `Robot_Module/module/chase.py` 中可调整：

```python
MAX_STEP_DISTANCE = 0.5  # 最大步长（米）
ARRIVAL_THRESHOLD = 5.0  # 到达阈值（像素）
```

### 追击算法特性

- **PID控制**: 距离越近步长越小，避免超调
- **精确定位**: 5像素以内才算到达
- **自动清除**: 追击完成后自动清除敌人
- **实时更新**: 通过ROS2话题获取实时位置

## 📝 依赖

```
openai>=1.0.0         # OpenAI API (兼容 Qwen/Dashscope)
fastmcp>=0.1.0        # MCP 服务器框架
pygame>=2.5.0         # 2D 仿真可视化
pyyaml>=6.0           # YAML 配置解析
numpy>=1.26.0         # 数值计算
python-dotenv>=1.0.0  # 环境变量管理
rclpy                 # ROS2 Python 客户端库
```

安装方法：
```bash
pip install -r requirements.txt
```

## 📚 API 参考

### 可用工具列表

#### 基础运动控制 (base.py)
- `move_forward(distance, speed)` - 前进
- `move_backward(distance, speed)` - 后退
- `turn(angle, angular_speed)` - 旋转
- `stop()` - 停止

#### 追击功能 (chase.py)
- `chase_enemy()` - 追击最近的敌人
- `get_enemy_positions()` - 获取所有敌人位置
- `chase_target(target_x, target_y)` - 追击指定坐标
- `chase_nearest_enemy(enemy_positions)` - 从列表中追击最近的
- `calculate_chase_angle(...)` - 计算追击角度

#### 视觉检测 (vision.py)
- `detect_color_and_act(image_path)` - 检测颜色并执行动作

## 💡 常见问题

### Q: 追击功能不工作？
A: 确保：
1. 仿真器已启动
2. 已生成敌人（鼠标点击）
3. API Key 已配置
4. ROS2 话题正常通信

### Q: 如何调试 ROS2 通讯？
A: 使用以下命令：
```bash
ros2 topic list                    # 查看所有话题
ros2 topic echo /robot/command     # 查看动作命令
ros2 topic echo /robot/enemies     # 查看敌人位置
ros2 topic echo /robot/state       # 查看机器人状态
```

### Q: 如何调整追击精度？
A: 修改 `Robot_Module/module/chase.py`：
```python
ARRIVAL_THRESHOLD = 5.0  # 减小此值提高精度
MAX_STEP_DISTANCE = 0.5  # 减小此值提高精度
```

---

**模块化，易扩展！** 🚀
