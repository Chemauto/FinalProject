# LLMPlanner 项目介绍

## 1. 项目概述

LLMPlanner 是一个面向腿足机器人 IsaacLab 仿真任务的自然语言规划系统。系统允许用户通过终端输入自然语言指令，例如“导航到目标点”“导航到 4,0 点”“绕过障碍物前进”等，随后自动完成环境观察、任务理解、技能队列生成、机器人技能调用、执行状态监控以及失败后的重新规划。

本项目定位于高层任务规划与技能调度层，不直接实现底层运动控制、关节控制或强化学习策略。底层仿真器脚本、训练好的策略模型、ROS2 状态桥接和机器人侧技能执行服务由独立仓库提供：

```bash
git clone https://github.com/Chemauto/llm-legged-lab.git
```

LLMPlanner 通过 WebSocket 与该仿真与控制服务通信，将大语言模型生成的离散技能命令发送给机器人服务端。机器人服务端负责具体运动执行、状态发布和技能完成判定；LLMPlanner 负责将人类语言目标转换为机器人可执行的技能队列，并根据执行反馈决定是否继续执行或重新规划。

## 2. 项目目标

LLMPlanner 的核心目标是构建一个可解释、可扩展、可评测的具身智能任务规划框架。项目重点解决以下问题：

1. 自然语言目标如何转换为机器人可执行的动作序列。
2. 大语言模型如何在有限技能集合中进行可靠规划。
3. 视觉语义和结构化机器人状态如何共同参与环境理解。
4. 技能执行失败或超时时，系统如何利用反馈重新规划。
5. 如何对不同场景下的任务成功率、执行时间和最终误差进行自动评测。

与直接让大模型输出底层控制量不同，LLMPlanner 将模型限制在高层决策空间。模型只能调用系统注册的观察、导航、行走、推箱和攀爬等技能，不能直接输出速度、力矩或关节命令。这种设计提高了系统安全性和可调试性，也使规划结果可以通过工具调用日志进行复现和分析。

## 3. 系统总体架构

系统整体采用分层结构：

```text
用户自然语言指令
  -> TUI 终端交互层
  -> Planner 高层任务规划层
  -> Vision 环境语义感知层
  -> Executor 技能执行与控制接口层
  -> WebSocket / ROS2 / IsaacLab 技能服务端
  -> 状态反馈与重规划
```

各层职责如下：

- `Tui/`：负责人机交互、命令解析、会话管理、计划展示和执行调度。
- `Planner/`：负责提示词加载、大模型调用、工具定义注入和 tool calls 解析。
- `Vision/`：负责视觉语言模型观察、机器人状态读取和结构化场景事实融合。
- `Executor/`：负责技能注册、参数封装、WebSocket 通信、状态缓存和队列执行。
- `scripts/`：负责自动化评测、结果汇总和论文图表生成。
- `eval_targets/`：保存固定场景的真值目标点。

这种结构将自然语言理解、环境感知、任务规划、技能执行和底层控制解耦。LLMPlanner 不直接依赖 ROS2 topic，也不直接控制仿真环境，而是通过稳定的 WebSocket 协议与外部技能服务端通信。

## 4. 仿真器与外部依赖

LLMPlanner 依赖 `llm-legged-lab` 提供仿真器和底层技能执行能力。该仓库包含：

- IsaacLab 仿真场景。
- 训练好的腿足机器人策略模型。
- ROS2 状态桥接。
- WebSocket 技能服务。
- 导航、行走、推箱、攀爬等底层执行逻辑。

典型部署方式是先启动 `llm-legged-lab` 中的 IsaacLab 仿真器和 WebSocket 技能服务，使其监听：

```text
ws://127.0.0.1:8765
```

然后启动 LLMPlanner 的 TUI。用户在 TUI 中输入任务后，LLMPlanner 通过 WebSocket 发送技能命令，服务端执行后返回实时状态和最终反馈。

## 5. 目录结构

```text
LLMPlanner/
├── README.md
├── Project.md
├── requirements.txt
├── eval_targets/
│   └── navigation_targets.json
├── Planner/
│   ├── llm_core.py
│   └── prompts/
│       └── planner_prompt.yaml
├── Vision/
│   ├── image_source.py
│   ├── vlm.py
│   ├── vlm_utils.py
│   └── prompts/
│       └── VlmPrompt.yaml
├── Executor/
│   ├── executor.py
│   ├── robot_ws.py
│   ├── skills.py
│   ├── state.py
│   └── tools.py
├── Tui/
│   ├── tui.py
│   ├── planning.py
│   ├── commands.py
│   ├── render.py
│   └── session.py
└── scripts/
    ├── evaluate_navigation.py
    ├── summarize_navigation_eval.py
    └── README.md
```

## 6. 环境语义感知层

环境语义感知层由视觉语言模型观察和结构化状态融合共同组成。系统不会只依赖图片描述进行规划，而是将 VLM 视觉语义与 WebSocket 返回的机器人状态、箱体位置和场景物体列表结合起来。

该层输入包括：

- 当前相机图像或图像路径。
- 机器人世界坐标、朝向和当前技能状态。
- 箱体世界坐标和相对坐标。
- 场景物体的中心点、尺寸和可移动属性。
- 视觉语言模型对图像的语义理解结果。

该层输出为规划层可使用的结构化事实，通常包括：

- 当前环境摘要。
- 障碍物、平台和箱体等地形要素。
- 左侧、右侧、中心线等通行选项。
- 前方通道是否被阻塞。
- 可交互物体及其目标位置。
- 机器人尺寸约束和最大攀爬高度。
- 视觉或状态融合中的不确定性。

这种设计的关键在于，系统显式考虑机器人尺寸。机器人不是点模型，导航坐标表示机器人中心点。当前项目中机器人近似尺寸为长 1.20m、宽 0.40m、高 0.30m，因此在接近障碍物前缘时，导航目标需要预留约 0.60m 的半身长度，避免目标点落入障碍物内部。

## 7. 高层任务规划层

高层任务规划层负责调用大语言模型。模型接收用户目标、系统提示词、历史上下文、观察结果和工具定义，并通过 tool calling 生成可执行动作。

Planner 层的主要功能包括：

- 读取模型名称、API Key 和 OpenAI 兼容服务地址。
- 加载系统提示词和任务规划规则。
- 将可用技能以工具定义形式提供给模型。
- 解析模型返回的自然语言计划和结构化 tool calls。
- 将规划结果交给 TUI 和 Executor。

当前规划层强调“先观察、再规划”。对于新的动作任务，系统会先调用观察工具，获取环境语义和结构化状态。观察完成后，模型需要一次性返回完整动作队列，而不是只返回第一步。

例如，在双侧高障碍且存在可推动箱体的场景中，合理队列可能是：

```text
push(1.7, 0, yaw=0)
nav(0.9, 0, 0.3)
climb(0.5)
nav(4.0, 0, 0.5)
```

这种队列式规划减少了每个子任务之间的重复模型调用，也避免了模型在执行过程中反复改变策略。只有当执行失败、超时或状态不确定时，上层模型才会重新介入。

## 8. 技能执行与控制接口层

技能执行层负责把模型输出的工具调用转换为 WebSocket 命令。当前系统支持的主要技能包括：

```text
observe(image_path="")              观察场景
nav(x, y, z)                        导航到目标位置
walk_skill(direction, v, distance)  按方向和距离行走
push(x, y, yaw)                     推动箱体到目标位姿
climb(height)                       攀爬指定高度
```

技能命令以统一格式发送：

```json
{
  "type": "command",
  "action_id": "nav-xxxx",
  "skill": "nav",
  "args": {"x": 4.0, "y": 0.0, "z": 0.3}
}
```

其中，`action_id` 用于唯一标识本次动作；`skill` 表示技能名称；`args` 保存技能参数。服务端执行技能时会持续推送状态，并在动作结束后返回反馈：

```json
{
  "type": "feedback",
  "action_id": "nav-xxxx",
  "skill": "nav",
  "signal": "SUCCESS",
  "message": "arrived"
}
```

执行层会按顺序执行模型返回的动作队列。如果某一步成功，则继续下一步；如果某一步失败或超时，则立即停止后续动作，并将最新状态和失败原因返回给规划层。

## 9. 状态反馈与重规划

机器人任务执行具有不确定性。导航可能被阻挡，推箱可能未到达目标，攀爬可能因为姿态不合适而失败。因此，LLMPlanner 不把模型计划视为一次性静态结果，而是将执行反馈纳入闭环。

反馈机制包括两类信息：

1. 实时状态：机器人位置、朝向、箱体位置、当前技能、场景物体等。
2. 最终反馈：动作是否成功、失败原因、动作标识和技能名称。

系统采用“成功不重思考，失败才重规划”的策略：

- 动作队列全部成功时，任务结束。
- 队列中任意动作失败时，停止后续动作。
- 技能超过时间上限时，按失败处理。
- 失败结果和最新状态会被重新输入给模型。
- 模型基于当前状态生成新的补救计划。

这种机制可以减少无效模型调用，同时使系统具备在线恢复能力。

## 10. 场景策略与机器人尺寸约束

LLMPlanner 面向多个固定导航场景。不同场景包含低障碍、高障碍、可推动箱体和被阻塞通道。系统规划时需要同时考虑目标点、障碍物尺寸、机器人尺寸和可用技能。

典型策略包括：

- 中心线畅通：直接导航到目标点。
- 单侧低障碍：优先选择可通行路线，必要时攀爬低障碍。
- 双侧低障碍：选择一侧平台，先接近前缘，再攀爬并继续导航。
- 一侧低障碍一侧高障碍：选择低障碍侧，避免不可直接攀爬的高障碍。
- 双侧高障碍且有箱体：先推箱到辅助位置，再借助箱体完成连续攀爬。

场景中的障碍物不是点，而是具有中心和尺寸的实体。规划层需要根据物体占用边界和机器人半长、半宽计算合理接近点。例如障碍物前缘位于 x=2.0m 时，机器人中心导航点应在 x=1.4m 左右，而不是直接导航到 x=2.0m。

## 11. TUI 交互

TUI 是用户主要入口。启动方式如下：

```bash
cd LLMPlanner
conda activate ros2_env
python Tui/tui.py
```

常用命令包括：

```text
/help      查看帮助
/connect   连接 WebSocket 技能服务
/load      恢复最近会话
/history   显示历史文件路径
/reset     清空上下文
/quit      退出
```

用户输入普通自然语言后，TUI 会将任务交给 Planner，显示模型计划，并把 tool calls 交给 Executor 执行。执行过程中，TUI 会显示工具调用、机器人状态、错误信息和最终结果。

## 12. 自动评测

项目提供自动评测脚本，用于统计不同场景下的任务成功率和误差。固定目标文件为：

```text
eval_targets/navigation_targets.json
```

运行评测：

```bash
python scripts/evaluate_navigation.py \
  --target-json eval_targets/navigation_targets.json \
  --scenes 0 1 2 3 4 \
  --trials 25 \
  --instruction "导航到目标点" \
  --threshold 0.3
```

评测结果保存到 `eval_results/`，该目录已被 `.gitignore` 忽略。结果字段包括：

- `passed`：是否通过误差阈值。
- `error_xy_m`：最终 XY 平面误差。
- `duration_sec`：任务总耗时。
- `planning_duration_sec`：大模型规划耗时。
- `execution_duration_sec`：技能执行和等待反馈耗时。
- `planner_calls`：模型规划调用次数。

按场景汇总并生成图表：

```bash
python scripts/summarize_navigation_eval.py eval_results/navigation_eval_xxx.md
```

旧报告没有规划时间字段时，可以手动指定：

```bash
python scripts/summarize_navigation_eval.py report.md --planning-times 30,40,45,43,44
```

## 13. 运行配置

项目可通过 `.env` 配置模型和机器人服务连接：

```env
MODEL_API_KEY=你的模型API Key
MODEL_BASE_URL=https://你的OpenAI兼容接口/v1
ROBOT_WS_URL=ws://127.0.0.1:8765
ROBOT_WS_TIMEOUT_SEC=60
ROBOT_WS_CONNECT_TIMEOUT_SEC=3
VISION_IMAGE_PATH=/path/to/current_camera.png
VISION_MODEL=qwen3-vl-flash-2026-01-22
```

安装依赖：

```bash
pip install -r requirements.txt
```

`requirements.txt` 使用版本范围约束，避免依赖升级到不兼容的大版本。

## 14. 项目特点

LLMPlanner 的主要特点包括：

- 采用模块化架构，规划、感知、执行和交互边界清晰。
- 大模型只负责高层任务规划，不直接输出底层控制量。
- 通过 tool calling 约束模型输出，使动作可验证、可复现。
- 融合 VLM 视觉语义和 WebSocket 结构化状态，提高环境理解稳定性。
- 显式考虑机器人尺寸、障碍物尺寸和通道净宽。
- 支持动作队列连续执行，减少重复模型调用。
- 支持失败反馈和在线重规划。
- 支持自动评测和论文图表生成。

## 15. 当前局限

当前版本仍存在一些局限：

- 场景规则主要面向固定 IsaacLab 导航场景，开放环境泛化能力有限。
- 底层技能成功判定依赖外部 WebSocket 服务端反馈。
- VLM 在结构化状态缺失时仍可能出现视觉误判。
- 当前主要关注导航、推箱和攀爬，尚未扩展到抓取、开门等复杂操作。
- TUI 更适合调试和演示，尚未提供图形化任务监控面板。

## 16. 后续扩展

后续可从以下方向扩展：

- 引入更通用的地图表示和路径规划器。
- 增加行为树或状态机，提高长任务稳定性。
- 增强失败恢复策略，例如自动尝试备用接近点。
- 标准化 WebSocket 技能协议，便于对接真实机器人。
- 增加任务日志记录和轨迹可视化。
- 扩展更多具身技能，例如抓取、放置、开门和精细对齐。
- 将 TUI 扩展为 Web 控制台。

## 17. 总结

LLMPlanner 构建了一个从自然语言输入到机器人技能执行的闭环系统。系统利用大语言模型进行任务理解和技能调度，利用视觉语言模型和结构化状态进行环境感知，再通过 WebSocket 调用底层机器人技能。它没有让模型直接控制机器人，而是让模型在受约束的技能空间中规划，并通过状态反馈和重规划机制提高任务执行稳定性。

该项目适合作为研究大模型、视觉感知、机器人技能执行和 IsaacLab 仿真协同工作的基础框架，也可作为后续具身智能任务系统的扩展起点。
