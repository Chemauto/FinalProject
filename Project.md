# FinalProject 项目介绍

## 1. 项目概述

FinalProject 是一个面向机器人任务执行的自然语言规划系统。项目的核心目标是让用户通过终端输入自然语言指令，例如“导航到目标点”“到达 5,0 点”“绕过障碍物前进”等，系统能够自动完成环境观察、任务规划、技能调度、机器人控制命令下发、执行状态监控以及失败后的重新规划。

该项目并不是一个直接控制电机、关节或速度的底层控制器，而是一个位于高层任务规划和机器人执行服务之间的智能规划框架。系统利用大语言模型理解用户意图，利用视觉语言模型和机器人状态服务理解环境，再通过 WebSocket 将离散技能命令发送给仿真或机器人控制端。底层机器人服务负责具体运动控制、ROS2 数据交互和技能执行结果判断；FinalProject 负责将“人类语言目标”转换为“机器人可执行的技能队列”。

从整体功能上看，项目完成了以下任务：

- 通过终端交互界面接收用户自然语言输入。
- 连接机器人 WebSocket 服务，读取当前机器人状态和场景物体信息。
- 调用视觉语言模型理解当前前视图像。
- 融合 VLM 视觉语义与 ROS2/WebSocket 返回的结构化环境状态。
- 将场景转换为包含障碍物、通道、箱体、机器人尺寸约束的结构化事实。
- 调用大语言模型进行任务规划，并通过 tool calling 输出可执行技能。
- 支持一次性生成完整动作队列，例如“导航到障碍物前缘 -> 攀爬 -> 继续导航”。
- 通过 WebSocket 向机器人服务端发送导航、行走、推箱、攀爬等技能命令。
- 接收机器人服务端实时状态和最终反馈。
- 在技能成功时连续执行队列，在失败或超时时停止并触发上层重规划。
- 在 TUI 中实时显示计划、工具调用、机器人状态、错误和最终结果。

项目当前处于重构后的最小可运行框架阶段，但已经具备完整的“感知 -> 规划 -> 执行 -> 反馈”闭环。与旧式脚本化任务执行相比，该项目更强调模块边界清晰、数据结构统一、工具调用可验证，以及上层模型只在必要时介入。

## 2. 项目设计目标

本项目的设计目标主要包括以下几个方面。

### 2.1 自然语言驱动机器人任务

传统机器人任务执行往往需要用户输入固定格式命令，例如指定精确坐标、调用具体脚本或手动选择技能。FinalProject 希望用户只需要以自然语言表达目标，系统就能自动理解任务含义，并结合当前环境决定应该执行哪些机器人技能。

例如，用户输入“导航到目标点”时，系统不会简单地直接向目标坐标导航，而是会先观察环境。如果中心线被障碍物阻挡，系统会判断是否需要绕行、攀爬或者借助箱体。如果用户输入“导航到 4,0 点”，系统也会根据当前场景判断直接导航是否可行，而不是盲目发送目标点。

### 2.2 让大模型做高层推理，而不是底层控制

大语言模型擅长处理开放语言理解、任务分解和规则推理，但不适合作为连续控制器直接输出底层速度、力矩或关节控制量。因此，本项目将大模型限制在高层任务规划阶段。模型只能调用系统提供的有限技能工具，例如观察、导航、行走、推箱和攀爬。

这种设计降低了模型输出危险控制指令的风险，也使任务执行过程更容易调试和验证。底层机器人如何移动、如何攀爬、如何推箱，仍然由机器人服务端和控制策略完成。FinalProject 只负责决定“什么时候调用哪个技能、参数是什么、失败后如何处理”。

### 2.3 融合视觉语义与结构化机器人状态

单纯依靠图像理解会存在不确定性。视觉语言模型可能受到视角、遮挡、光照和模型幻觉影响，导致障碍物位置、尺寸或通行条件判断不稳定。因此，项目设计中将 VLM 输出作为语义补充，将 ROS2/WebSocket 返回的结构化状态作为几何决策的优先依据。

系统会读取机器人位置、箱体位置、场景物体列表、当前技能状态等信息，并将其融合进统一的 `scene_facts`。这样，规划层看到的不只是自然语言描述，而是包含物体中心、尺寸、通道状态、机器人尺寸约束和可交互对象的结构化环境事实。

### 2.4 支持队列式技能执行

早期模型规划容易出现一个问题：模型在正文中写出了完整的多步计划，但 tool calls 只调用第一步。这样系统每执行一个子任务都要重新调用模型，容易造成重复思考、计划漂移和执行延迟。

当前版本改为队列式规划和执行。观察完成后，如果任务需要多个动作，模型必须一次性生成完整动作队列。执行器会按顺序执行队列，只有出现失败、超时或状态不确定时，才重新交给上层模型介入。这种方式更接近传统任务规划中的顺序计划执行，也能减少模型调用次数。

### 2.5 建立失败反馈与重规划闭环

机器人任务执行并不总是成功。导航可能被障碍物阻挡，推箱可能没有到达目标，攀爬可能因为位置或姿态不合适而失败。FinalProject 不假设计划一定成功，而是通过 WebSocket 持续接收执行状态和最终反馈。

当动作队列中的某一步失败或超时时，执行器会停止后续步骤，并将失败信息、最新机器人状态和执行摘要返回给规划层。模型再根据当前状态重新规划。这种机制使系统从单次静态计划变成在线闭环执行系统。

## 3. 总体架构

项目整体可以划分为五个主要部分：

```text
用户终端输入
  -> TUI 交互层
  -> Planner 任务规划层
  -> Vision 环境语义感知层
  -> Executor 技能执行层
  -> WebSocket / ROS2 机器人服务端
  -> 状态反馈返回 TUI 和 Planner
```

其中，FinalProject 自身不直接订阅 ROS2 topic，也不直接运行底层控制器。ROS2 通信、仿真状态读取和机器人控制逻辑由外部机器人服务端负责。FinalProject 通过 WebSocket 与该服务端通信。

系统的典型执行流程如下：

1. 用户在 TUI 中输入自然语言任务。
2. TUI 将用户消息加入会话上下文。
3. Planner 调用大语言模型，并提供工具定义。
4. 对于新的动作任务，系统强制先执行环境观察。
5. Vision 调用视觉语言模型，并读取 WebSocket 返回的机器人状态。
6. Vision 将视觉语义和结构化状态融合为 `scene_facts`。
7. Planner 基于观察结果生成动作队列。
8. Executor 按顺序执行动作队列中的每个技能。
9. 每个技能通过 WebSocket 发送给机器人服务端。
10. 机器人服务端持续推送状态，并在技能结束时返回反馈。
11. Executor 根据反馈判断继续执行、停止或失败。
12. 若队列全部成功，任务结束；若失败或超时，将结果交回 Planner 重规划。

这种架构的核心思想是分层解耦。TUI 负责人机交互，Planner 负责模型调用，Vision 负责环境事实生成，Executor 负责技能执行，机器人服务端负责真实控制。每一层只处理自己边界内的问题，减少模块之间的耦合。

## 4. 目录结构说明

当前项目目录主要包含以下模块：

```text
FinalProject/
├── README.md
├── Project.md
├── Planner/
│   ├── README.md
│   ├── llm_core.py
│   └── prompts/
│       └── planner_prompt.yaml
├── Vision/
│   ├── README.md
│   ├── __init__.py
│   ├── image_source.py
│   ├── vlm.py
│   ├── vlm_utils.py
│   └── prompts/
│       └── VlmPrompt.yaml
├── Executor/
│   ├── README.md
│   ├── __init__.py
│   ├── robot_ws.py
│   ├── state.py
│   ├── skills.py
│   ├── executor.py
│   └── tools.py
├── Tui/
│   ├── README.md
│   ├── commands.py
│   ├── gateway.py
│   ├── history.py
│   ├── planning.py
│   ├── render.py
│   ├── session.py
│   ├── stream.py
│   └── tui.py
└── tests/
```

各目录职责如下：

- `Planner/`：负责大语言模型调用、提示词加载和 tool calling 规划。
- `Vision/`：负责图像理解、机器人状态读取和结构化场景事实构建。
- `Executor/`：负责工具注册、技能命令封装、WebSocket 通信、状态缓存和动作队列执行。
- `Tui/`：负责终端交互、命令解析、会话管理、计划显示和运行时调度控制。
- `tests/`：负责关键逻辑的单元测试，包括技能接口合同、队列执行逻辑和提示词约束检查。

## 5. Planner 任务规划层

Planner 是系统的大模型调用层。它的主要职责是读取提示词配置，加载模型名称，构造 OpenAI 兼容接口请求，并在需要时附带工具定义，让模型通过 tool calling 返回可执行计划。

该层主要处理以下工作：

- 从环境变量读取模型 API Key 和模型服务地址。
- 加载项目中的系统提示词。
- 构造聊天消息。
- 在有工具定义时启用 tool calling。
- 将模型返回的工具调用解析为统一结构。
- 将模型正文中的计划说明保留下来，交给 TUI 显示。

Planner 本身不直接执行机器人动作，也不直接读取图像或 ROS2 状态。它只负责把当前会话上下文、系统提示词和工具定义提交给模型，并返回模型规划结果。

当前提示词中包含了较多机器人任务约束。例如：

- 每个新动作任务必须先观察。
- 机器人导航坐标按机器人中心点计算。
- 障碍物不是点，而是有尺寸的物体。
- 前方中心线阻塞时不能直接向中间点导航。
- 双侧低障碍时应选择可攀爬路径。
- 双侧高障碍且存在箱体时，需要推箱辅助攀爬。
- 多步任务应一次性返回完整动作工具序列。
- 单个技能执行上限为 30 秒，失败或超时后再重规划。

这些规则的作用是将大语言模型的开放推理约束到项目实际可执行的技能空间内，使模型输出更稳定、更符合机器人运动约束。

## 6. Vision 环境语义感知层

Vision 是系统的环境理解层。它负责把相机图像和机器人实时状态转换为规划层可使用的结构化环境事实。

Vision 的输入主要包括：

- 前视相机图像。
- 当前机器人位姿。
- 当前箱体世界坐标和相对坐标。
- 场景物体列表。
- 当前机器人技能状态。
- VLM 对图像的结构化描述。

Vision 的输出主要包括：

- `visual_context`：视觉语言模型给出的图像语义理解结果。
- `robot_state`：从 WebSocket 读取并标准化后的机器人状态。
- `scene_facts`：融合视觉语义和结构化状态后的环境事实。

`scene_facts` 是后续规划中最重要的数据。它通常包含以下字段：

- `summary`：对当前环境的简要总结。
- `terrain_features`：障碍物、平台等地形要素。
- `interactive_objects`：可推动箱体等可交互物体。
- `route_options`：左侧、右侧、前方等方向的通行选项。
- `corridors`：前方通道状态和净宽判断。
- `box_step_goal`：可移动箱体作为辅助台阶时的目标位置。
- `constraints`：机器人尺寸、最大攀爬高度、导航坐标系等约束。
- `uncertainties`：视觉或状态融合中的不确定性说明。
- `visual_summary`：保留 VLM 原始摘要作为语义补充。

该层最重要的工程细节是机器人尺寸建模。系统不会把机器人看作一个点，而是显式记录机器人长、宽、高、半长和半宽。导航坐标被定义为机器人中心点。对于障碍物和箱体，系统根据中心点和尺寸计算占用边界，再根据机器人尺寸判断是否可通过。

例如，当前障碍物占据 x=2.0 到 x=6.0 区间，如果机器人需要在攀爬前接近障碍物前缘，则机器人中心点不能直接导航到 x=2.0，而应停在前缘之前约半个机身长度的位置，即 x=1.4 左右。类似地，如果左右两个障碍物之间的净宽小于机器人所需宽度，系统会将前方通道标记为不可直接通过。

这种结构化几何判断可以避免模型仅凭文字描述误判路线。例如，当左右两侧各有一个宽 1.5m 的障碍物，而走廊总宽度为 3.0m 时，虽然图像中可能看起来中间有缝隙，但从尺寸上看中心线实际上被完全阻塞，机器人无法直接通过。

Vision 还具备降级能力。如果 WebSocket 状态读取失败，观察流程不会直接崩溃，而是退回 VLM 视觉结果，并标记机器人状态未连接。这样系统在状态服务不可用时仍能给出基本环境描述，在状态服务恢复后再使用更可靠的结构化事实。

## 7. Executor 技能执行层

Executor 是系统的技能执行和机器人通信层。它负责把 Planner 输出的工具调用转换为机器人服务端能够理解的 WebSocket 命令，并接收执行期间的状态和最终反馈。

Executor 主要由以下部分构成：

- 工具注册：将观察、导航、行走、推箱、攀爬等能力注册为模型可调用工具。
- 技能封装：为每类机器人技能提供统一的参数入口。
- WebSocket 通信：构造命令消息，发送到机器人服务端，并等待反馈。
- 状态缓存：保存服务端推送的最新机器人状态和反馈。
- 队列执行：按顺序执行模型返回的多个 tool calls。
- 失败处理：某一步失败时停止队列，并返回失败信息。

当前系统支持的主要技能包括：

```text
observe(image_path="")
nav(x, y, z)
walk_skill(direction, v, distance)
push(x, y, yaw)
climb(height)
```

这些技能并不在 FinalProject 内直接实现底层运动控制。FinalProject 只是向机器人服务端发送启动信号，例如发送一个导航目标或攀爬高度。真正的控制策略、运动执行和物理状态判断由服务端完成。

WebSocket 命令通常包含以下内容：

```json
{
  "type": "command",
  "action_id": "nav-xxxx",
  "skill": "nav",
  "args": {
    "x": 5,
    "y": 0,
    "z": 0.3
  }
}
```

其中，`action_id` 用于区分不同动作，保证客户端接收到反馈时能够确认该反馈对应当前命令。服务端执行过程中会不断发送状态消息，最终返回反馈消息：

```json
{
  "type": "feedback",
  "action_id": "nav-xxxx",
  "skill": "nav",
  "signal": "SUCCESS",
  "message": "arrived"
}
```

Executor 会将不同工具的返回值归一化为统一结构，包括工具名、参数、成功或失败信号、消息文本和原始结果。这样 TUI 和 Planner 不需要关心每个技能的底层返回格式。

当前版本为每个单独技能设置了 30 秒执行上限。如果服务端在规定时间内没有返回最终反馈，客户端会将该动作视为失败。该设计可以避免系统长时间卡在某个技能上，也使失败能够及时进入重规划流程。

Executor 的另一个关键点是参数语义统一。当前推箱技能的第三个参数是目标朝向 `yaw`，不是箱体高度；攀爬技能普通场景支持 0.3m 高度，借助箱体的特殊场景可使用 0.5m 表示连续攀爬。通过这种参数约束，可以减少模型输出错误参数导致底层执行失败的概率。

## 8. TUI 终端交互层

TUI 是用户直接使用的入口。用户在终端运行 `python tui.py` 后，可以通过命令连接机器人服务、输入自然语言任务、查看模型规划、观察工具调用和实时状态反馈。

TUI 提供以下常用命令：

```text
/help     查看帮助
/connect  检查机器人 WebSocket 服务连接
/load     恢复最近会话
/history  显示历史文件路径
/reset    清空当前上下文
/quit     退出
```

普通自然语言输入会进入规划执行流程。TUI 会把用户输入加入模型会话，调用 Planner 获取计划，显示模型的计划正文，然后将 tool calls 交给 Executor 执行。

TUI 的显示内容主要包括：

- 用户输入。
- 系统消息。
- 模型计划。
- 工具调用。
- 实时状态。
- 错误信息。
- 连接状态。

为了避免机器人状态高频刷新导致终端刷屏，TUI 对 `status` 类型消息采用覆盖式更新。也就是说，机器人持续运动时，界面只更新最后一条状态，而不是每次状态推送都新增一行。这样可以让用户持续看到机器人当前位置、当前技能和箱体状态，同时保持界面清晰。

当前 TUI 中还包含运行时规划约束注入逻辑。该逻辑会在调用模型前临时添加一条当前阶段约束：

- 如果本轮任务尚未观察环境，则只允许调用观察工具。
- 如果已经观察完成，并且任务需要多个动作，则要求模型一次性返回完整动作队列。

这条运行时约束不会写入永久会话历史，只影响当前一轮模型调用。它的作用是让模型在生成计划正文和 tool calls 时就知道执行器支持队列连续执行，从源头减少“只调用第一步”的问题。

TUI 还负责判断执行后是否需要再次调用模型。如果动作队列全部成功，TUI 会显示“动作队列执行完成”，不会每个子任务结束后都重新请求模型。如果某一步失败或超时，TUI 会把执行摘要和最新状态交回模型，要求模型决定下一步。

## 9. 状态反馈与重规划机制

状态反馈是该项目区别于简单聊天机器人的关键部分。机器人任务不是一次性文本生成，而是需要在物理环境中执行。执行结果可能成功，也可能失败。因此，系统必须将执行反馈重新纳入规划过程。

当前系统中的反馈主要来自 WebSocket 服务端。服务端会推送两类信息：

1. 实时状态：机器人位置、朝向、箱体位置、当前技能、启动状态、场景物体等。
2. 最终反馈：某个动作是否成功、失败原因、动作标识和技能名。

Executor 会把实时状态保存到本地缓存，并通过 TUI 显示。技能执行结束后，最终反馈会被整理为结构化结果。TUI 再根据结果判断是否继续执行队列。

系统当前采用“成功不重思考，失败才重规划”的原则：

- 如果队列中的动作全部成功，系统直接结束本轮任务。
- 如果某一步失败，执行器停止后续动作。
- 如果某一步超时，也按失败处理。
- 失败结果会连同最新机器人状态一起传回模型。
- 模型基于当前状态生成新的补救计划。

这种机制可以减少无意义的大模型调用。对于确定性的动作队列，没必要每一步都重新让模型思考。只有当真实环境与计划预期不一致时，才需要模型根据新状态重新判断。

例如，在一个需要攀爬的任务中，如果模型生成了“导航到平台前缘 -> 攀爬 -> 导航到目标”的完整队列，并且导航成功、攀爬成功，则系统会继续执行后续导航，不会在每一步成功后暂停给模型。如果导航到平台前缘失败，系统会停止队列，并把失败位置、机器人当前状态和错误信息交给模型，模型可以重新选择接近点或改走另一侧。

## 10. 机器人尺寸与场景约束

当前项目对机器人尺寸和场景几何进行了显式建模，这是系统能否正确规划的关键。

机器人近似尺寸为：

```text
长度：1.20m
宽度：0.40m
高度：0.30m
半长：0.60m
半宽：0.20m
```

导航坐标表示机器人中心点，而不是机器人前端。因此，当机器人要接近障碍物前缘时，中心点必须停在障碍物前缘前方约 0.60m 的位置。否则，如果直接把导航目标设在障碍物边缘甚至障碍物内部，机器人会在物理上发生碰撞或无法到达。

场景中常见障碍物包括：

- 低障碍：高度约 0.3m，可直接攀爬。
- 高障碍：高度约 0.5m，普通情况下不可直接攀爬。
- 可移动箱体：可被推动到指定位置，作为辅助台阶。
- 左右平台：可能共同阻塞走廊中心线。

在固定场景中，走廊宽度约 3.0m，左右两个障碍物如果各占 1.5m 宽，则中心线没有足够通行空间。系统会根据物体 `center` 和 `size` 计算占用边界，再判断机器人是否可以通过。

对于不同场景，系统采用不同策略：

- 中心线畅通：直接导航到目标点。
- 单侧低障碍：若另一侧通道可行，优先绕行；若必须翻越，则接近低障碍并攀爬。
- 双侧低障碍：选择一侧低障碍，导航到前缘，攀爬，再继续导航。
- 一侧低障碍一侧高障碍：选择低障碍方向，避免不可攀爬高障碍。
- 双侧高障碍且有箱体：先推箱到辅助位置，再借助箱体完成连续攀爬。

这些规则并不是为了替代模型，而是为模型提供明确的物理约束。模型仍然负责结合用户目标和当前观察结果选择动作队列，但必须在这些约束范围内进行规划。

## 11. 当前典型任务示例

### 11.1 直接导航任务

当用户输入“导航到 5,0 点”，且观察结果显示前方中心线畅通时，系统可以直接生成导航动作。执行器发送目标坐标给机器人服务端，服务端控制机器人移动，并返回成功或失败反馈。

### 11.2 双侧低障碍越障任务

当观察结果显示左右两侧均存在 0.3m 低平台，中心线被阻塞，但低平台可攀爬时，系统不会直接导航到中心线目标点，而是生成类似以下逻辑的队列：

```text
导航到低平台前缘并对齐平台中心
-> 攀爬 0.3m 平台
-> 导航到目标点
```

这个队列会一次性返回给执行层，由执行层连续执行。

### 11.3 单侧低障碍、单侧高障碍任务

当一侧是 0.3m 低平台、另一侧是 0.5m 高平台时，系统会避免选择高平台方向，因为机器人普通攀爬能力不足。此时合理策略是选择低平台侧，先接近低平台，再执行攀爬和后续导航。

### 11.4 双侧高障碍和推箱任务

当左右两侧都是 0.5m 高障碍，并且存在可移动箱体时，普通攀爬无法直接通过。系统会采用推箱辅助策略：

```text
推箱到辅助位置
-> 导航到箱体后方合适接近点
-> 借助箱体连续攀爬到高平台
-> 导航到最终目标
```

该任务体现了系统对“可交互物体”的利用。箱体不只是被识别为物体，还被规划为改变环境可达性的工具。

## 12. WebSocket 与 ROS2 的关系

FinalProject 不直接导入 ROS2，也不在本项目内部订阅 ROS2 topic。这样做是为了减少任务规划代码对 ROS2 环境的强依赖。ROS2 相关逻辑位于外部机器人服务端，FinalProject 通过 WebSocket 与其通信。

这种架构有几个优点：

- 规划系统可以在普通 Python 环境中运行。
- ROS2 节点、仿真环境和控制器可以独立启动和调试。
- WebSocket 协议清晰，便于日志记录和故障定位。
- 后续替换仿真环境或真实机器人时，只要保持协议一致，FinalProject 不需要大规模修改。

WebSocket 服务端需要支持以下能力：

- 健康检查。
- 读取当前状态。
- 接收技能命令。
- 推送实时状态。
- 返回最终反馈。

FinalProject 侧则负责构造命令、解析状态、维护本地缓存和驱动上层规划。

## 13. 环境变量与配置

项目通过 `.env` 或系统环境变量读取模型和机器人连接配置。常用配置包括：

```env
MODEL_API_KEY=你的API Key
MODEL_BASE_URL=OpenAI兼容接口地址
ROBOT_WS_URL=ws://127.0.0.1:8765
ROBOT_WS_TIMEOUT_SEC=60
ROBOT_WS_CONNECT_TIMEOUT_SEC=3
VISION_IMAGE_PATH=/path/to/current_camera.png
VISION_MODEL=qwen3-vl-flash-2026-01-22
```

其中，`MODEL_API_KEY` 和 `MODEL_BASE_URL` 用于连接 OpenAI 兼容的大语言模型服务；`ROBOT_WS_URL` 用于连接机器人 WebSocket 服务端；`VISION_IMAGE_PATH` 用于指定当前视觉输入图片；`VISION_MODEL` 用于配置视觉语言模型。

## 14. 运行方式

典型运行流程如下。

首先启动仿真环境：

```bash
cd /home/robot/work/IsaacLabBisShe
conda activate env_isaaclab
python Ros2/FinalSim.py --scene_id 4 --enable_front_camera
```

然后启动 ROS2 状态发布：

```bash
cd /home/robot/work/IsaacLabBisShe
conda activate ros2_env
source /opt/ros/jazzy/setup.bash
python Ros2/PublishRos2Topic.py
```

再启动 WebSocket 机器人服务：

```bash
cd /home/robot/work/IsaacLabBisShe
conda activate ros2_env
source /opt/ros/jazzy/setup.bash
python WebSocket/robot_service.py
```

最后启动 FinalProject 的 TUI：

```bash
cd /home/xcj/work/Planner/FinalProject/Tui
conda activate ros2_env
python tui.py
```

进入 TUI 后，可以先输入：

```text
/connect
```

确认机器人服务端在线后，再输入自然语言任务。

## 15. 测试与验证

项目中包含单元测试，用于验证关键逻辑。当前测试覆盖重点包括：

- 推箱技能参数是否使用 `yaw` 而不是旧的高度参数。
- 工具定义是否只暴露当前可执行技能。
- 攀爬高度约束是否符合普通攀爬和箱体辅助攀爬要求。
- 观察结果中是否包含机器人尺寸约束。
- 提示词是否保留关键的尺寸计算和场景规划规则。
- 多步计划正文与 tool calls 不一致时是否能被识别。
- 成功动作队列是否避免不必要的重新规划。
- 失败动作是否触发重规划。
- 运行时约束是否在观察前和观察后正确注入。

常用验证命令：

```bash
python3 -m unittest discover -s tests
python3 -m py_compile Executor/skills.py Executor/tools.py Vision/__init__.py Planner/llm_core.py Tui/tui.py Tui/planning.py
```

这些测试可以在不启动完整仿真环境的情况下检查规划接口、技能合同和队列控制逻辑。

## 16. 项目特点

FinalProject 当前具有以下特点：

第一，项目采用清晰的分层架构。TUI、Planner、Vision、Executor 和机器人服务端职责明确，便于调试和后续扩展。

第二，项目将大模型限制在高层规划空间。模型通过工具调用选择离散技能，不直接控制底层运动，从而提高安全性和可验证性。

第三，项目融合 VLM 语义和 ROS2 结构化状态。视觉模型负责理解图像，结构化状态负责几何判断，两者互补。

第四，项目显式考虑机器人尺寸。导航点、障碍物边界、通道宽度和攀爬高度都结合实际机器人尺寸处理，避免把机器人当成点。

第五，项目支持动作队列连续执行。模型观察后一次性生成完整队列，执行器按顺序执行，减少重复调用模型。

第六，项目具备失败反馈和重规划能力。动作失败或超时时，系统会停止队列并让模型根据最新状态重新规划。

第七，项目通过 WebSocket 解耦规划端和机器人端。FinalProject 不直接依赖 ROS2 节点，可以更灵活地与仿真或真实机器人服务连接。

## 17. 当前局限

虽然项目已经具备完整闭环，但当前版本仍有一些局限。

首先，场景规则仍然较依赖已知固定布局。当前提示词和感知层对几个典型障碍场景有较强约束，适合课程项目和固定仿真场景，但在完全开放环境中还需要更通用的地图构建和路径规划能力。

其次，底层技能成功与失败依赖机器人服务端反馈。如果服务端状态判断阈值不合理，可能出现实际接近目标但反馈失败，或反馈延迟导致客户端超时的问题。

第三，VLM 图像理解仍然可能存在误差。虽然系统优先使用结构化状态，但当结构化状态缺失时，视觉描述仍可能影响规划质量。

第四，当前任务规划主要基于规则提示和 tool calling，没有引入传统搜索式任务规划器。对于更长时序、更复杂依赖关系的任务，后续可以考虑结合符号规划、行为树或状态机。

第五，当前 TUI 主要面向调试和演示，尚未提供图形化界面、轨迹可视化和任务日志分析面板。

## 18. 后续扩展方向

后续可以从以下方向继续扩展项目：

- 引入更通用的地图表示，使系统不只适用于固定场景。
- 将障碍物、箱体和机器人状态可视化，提供更直观的调试界面。
- 增加行为树或有限状态机，使长任务执行更稳定。
- 增强失败恢复策略，例如自动尝试备用接近点、备用路线或重新观察。
- 将 WebSocket 协议进一步标准化，便于对接真实机器人。
- 增加任务日志记录，保存每次观察、计划、执行和反馈过程。
- 引入路径规划器，用几何规划结果辅助大模型选择技能。
- 增加更多技能，例如开门、抓取、放置、精细对齐等。
- 对不同模型进行对比测试，评估规划成功率、调用次数和失败恢复能力。
- 将当前 TUI 扩展为 Web 控制台，支持实时状态图、场景图和动作队列展示。

## 19. 总结

FinalProject 是一个以大模型为高层规划核心、以视觉和机器人状态为环境输入、以 WebSocket 技能接口为执行通道的机器人任务规划系统。它通过模块化设计实现了自然语言输入到机器人技能执行的闭环流程。

项目的关键价值在于，它没有让大模型直接控制机器人，而是让模型在受约束的技能空间中进行任务分解；它也没有单纯依赖视觉描述，而是融合了 ROS2/WebSocket 返回的结构化状态；它还通过动作队列、超时控制和失败重规划机制，使机器人任务执行更加稳定。

从工程角度看，该项目已经形成了一个清晰的机器人智能体框架：用户通过 TUI 输入任务，Planner 调用大语言模型生成计划，Vision 提供结构化环境事实，Executor 执行动作队列，WebSocket 服务端完成底层控制，状态反馈再回到上层形成闭环。该框架适合继续扩展为更复杂的具身智能任务系统，也适合作为课程项目中展示大模型、视觉感知、ROS2 仿真和机器人技能执行协同工作的基础平台。
